# Url which will be used as root for all cgit links
virtual-root=/git/

# Url which specifies the source of an image which will be used as a logo on all cgit pages
logo=/git/cgit.png

# Url which specifies the css document to include in all cgit pages
css=/git/cgit.css

# Scan this directory for any git projects
scan-path=/opt/repositories

enable-index-links=1
enable-log-filecount=1
enable-log-linecount=1

# Repository configuration
#repo.url=cgit.git
#repo.path=/opt/repositories/cgit.git
#repo.desc=Cgit Repo
